export default function Home() {
  return (
    <main style={{padding: '2rem', fontFamily: 'system-ui'}}>
      <h1>Event Hosting — Next.js scaffold</h1>
      <p>
        Greenfield App Router starter. Production UI lives in <code>frontend/</code>.
        API: Laravel backend at <code>backend/</code>.
      </p>
    </main>
  );
}
